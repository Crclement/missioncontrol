{
  "name": "missioncontrol",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time dashboard for monitoring Claude Code sessions",
  "workspaces": [
    "packages/shared",
    "packages/agent",
    "packages/dashboard"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:agent\" \"npm run dev:dashboard\"",
    "dev:agent": "npm run dev -w packages/agent",
    "dev:dashboard": "npm run dev -w packages/dashboard",
    "build": "npm run build -w packages/shared && npm run build -w packages/agent && npm run build -w packages/dashboard",
    "test": "npm run test -w packages/shared && npm run test -w packages/agent && npm run test -w packages/dashboard",
    "lint": "npm run lint -w packages/dashboard"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
